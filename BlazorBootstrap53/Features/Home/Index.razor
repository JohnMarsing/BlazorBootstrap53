@page "/"

@* 
@using BlazorBootstrap53.Features.Home.OffcanvasAutoClose.Enums 
       BlazorBootstrap53.Features.Home.OffcanvasAutoClose.Enums
*@

@using BlazorBootstrap53.Features.Home.OffcanvasAutoClose.Enums

<PageTitle>Home | Blazor 5.3</PageTitle>

<div class="pb-2 mt-4 mb-2 border-bottom">
	<h2>Home</h2>
</div>


@* <div class="card">
	<div class="card-header">
		Bootstrap 5.3 Test Bed
	</div>
	<div class="card-body">
		<h5 class="card-title">Home Page Content</h5>
		<p class="card-text">ToDo: Get my Bootstrap Offcanvas Menu component to work correctly i.e. close itself after an item has been selected</p>
	</div>
</div> *@

<p>
	I need a Blazor JS Interop example that will close a Bootstrap offcanvas component
</p>



<div class="card mb-3">
	<div class="card-body d-flex justify-content-end mx-1">
		@* <Filter CurrentFilter=@CurrentFilter OnFilterSelected="@ReturnedFilter" /> *@
		<BlazorBootstrap53.Features.Home.OffcanvasAutoClose.Filter CurrentFilter=@CurrentFilter OnFilterSelected="@ReturnedFilter"></BlazorBootstrap53.Features.Home.OffcanvasAutoClose.Filter>
	</div>
</div>


@* <BookSection NavItem="CurrentFilter" /> *@
<BlazorBootstrap53.Features.Home.OffcanvasAutoClose.BookSection NavItem="CurrentFilter" />

@code {
	public NavItem CurrentFilter { get; set; } = NavItem.HebrewPassOverOrCrossOver;
	

	#region EventCallbacks
	private void ReturnedFilter(NavItem filter)
	{
		CurrentFilter = filter;
		StateHasChanged();
	}
	#endregion
}